# ATLAS DevOS / EVA-X — PRD (Excerpt for May 19, 2026)

Full PRD is preserved at `/app/memory/PRD.md` (335 lines, untouched).
This file logs **what shipped on May 19, 2026** that the PRD didn't yet
reflect at boot time.

## Shipped this session

### 1. Project recovery
GitHub repo `svetlanaslinko057/12e2s211` was the source of truth; local
`/app` was empty after rollback. Full sync restored 26 916 LoC backend,
241-file web, 82-screen Expo app.

### 2. Estimate → project-booting unification (mobile + web)
Single helper `createProjectAndBoot()` shared across web `AuthedCta` and
`InlineSignup`. Mobile `handleRegister()` now chains to
`createProjectDirect()` so guests don't double-click. Web `/project-booting`
page created (mirrors mobile timeline). Bundle hash `main.e3b95ceb.js`.

### 3. Web build re-shipped under `/api/web-ui/*`
`yarn install` + `yarn build` re-rendered `/app/web/build/` (528.84 kB gzipped
main bundle). FastAPI serves the fresh CRA artifact.

### 4. Runtime-client migration completed
`src/api.ts` rewritten as a behavioural shim over `runtime` (preserves
axios-shaped surface; routes every call through runtime middleware:
token-prime, telemetry, auth-expired, dedup, capability-gate, retry).
All 43 legacy `import api from '@/src/api'` callsites continue to work
without modification. See `audit/RUNTIME_CLIENT_MIGRATION_COMPLETE.md`.

### 5. Expo Admin parity expansion (8 read-mostly drill-downs)
Operations grid added below the cockpit on `/admin/home`. New routes:
`/admin/users`, `/admin/team`, `/admin/contracts`, `/admin/templates`,
`/admin/integrations`, `/admin/inbox`, `/admin/marketplace`,
`/admin/master`. All `href: null` in the tab layout (drill-down only).
Shared primitives live in `src/admin/ui.tsx` and `src/admin/useAdminResource.ts`.
See `docs/product-scope-freeze-amend-1.md` for the architectural amendment.

## What is still cockpit-only (unchanged)

The admin tab bar is still **5 surfaces**: Home, QA, Validation, Finance,
Profile. Write actions (suspend user, rotate integration key, edit
contract, publish template, decompose-template authoring, kanban DnD)
remain canonical on web admin. Per the amendment, Expo admin sees
signal + reads; web admin owns mutations.

## Tester capability — already shipped, no longer in roadmap

The "Stage 4 Tester surface" item from earlier PRD revisions was retired
during the May 2026 pivot: validator/tester is an **opt-in capability**
on user accounts, surfaced inside `client/validation/*` and
`developer/validation/*`. The legacy `/tester/*` paths are thin redirects
preserved only for stable deep-links.

## Live system

- Backend: 608 routes, all `/api/*` prefixed. AUTO_BALANCER + EVENT
  ENGINE + OPERATOR SCHEDULER + GUARDIAN + MODULE MOTION loops running.
- Web UI: served by FastAPI at `/api/web-ui/*` from `/app/web/build`.
- Expo: tunnel preview at the standard Emergent preview URL, 5-tab admin
  cockpit + 8 drill-down screens accessible.
- Integrations: all in MOCK mode until keys are provided (per user
  direction).
